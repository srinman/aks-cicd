apiVersion: v1
kind: ServiceAccount
metadata:
  name: argocd-manager
  namespace: argocd-managed
  annotations:
    # This will be overridden by environment overlays with actual hub identity client ID
    azure.workload.identity/client-id: "REPLACE_WITH_HUB_IDENTITY_CLIENT_ID"
automountServiceAccountToken: true