apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: spoke-bootstrap-base

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml

commonLabels:
  app.kubernetes.io/name: spoke-bootstrap
  app.kubernetes.io/managed-by: argocd